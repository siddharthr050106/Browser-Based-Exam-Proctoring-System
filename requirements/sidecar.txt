# BEZP Sidecar — Python dependencies
# The sidecar uses the SAME detection pipeline as the original worker.
# These are the same as detection.txt since the sidecar wraps it unchanged.

# Core detection pipeline
mediapipe>=0.10.30
opencv-python-headless>=4.9.0
ultralytics>=8.2.0
scikit-image>=0.23.2
insightface>=0.7.3
numpy>=1.26.4
scikit-learn>=1.5.0

# Sidecar server
fastapi==0.111.0
uvicorn[standard]==0.30.0
structlog==24.1.0
websockets==12.0
pydantic-settings==2.2.1
